local ReplicatedStorage = game:GetService("ReplicatedStorage")

local QuBlox = require(ReplicatedStorage.QuBlox)
local Circuit = QuBlox.Circuit(3, 3)

Circuit:h(1)
Circuit:h(2)
Circuit:h(3)

local results = Circuit:results(100000)

print(results)
